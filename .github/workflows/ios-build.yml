# GitHub Actions workflow for CribAlert iOS app
# Validates that the app builds successfully on macOS

name: iOS Build & Validate

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'CribAlert/**'
      - '.github/workflows/ios-build.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'CribAlert/**'
  workflow_dispatch:  # Allow manual trigger

jobs:
  build:
    name: Build iOS App
    runs-on: macos-14  # macOS Sonoma with Xcode 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show available Xcode versions
        run: ls -la /Applications/ | grep Xcode

      - name: Select Xcode version
        run: |
          # Use the latest available Xcode 15.x
          if [ -d "/Applications/Xcode_15.4.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.3.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.3.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.2.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.2.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.1.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.1.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.0.1.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.0.1.app/Contents/Developer
          elif [ -d "/Applications/Xcode.app" ]; then
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          fi

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Show available simulators
        run: xcrun simctl list devices available | head -50

      - name: Install xcpretty
        run: gem install xcpretty

      - name: Build iOS App (Debug)
        run: |
          cd CribAlert
          
          # Find an available iPhone simulator
          SIMULATOR=$(xcrun simctl list devices available | grep "iPhone" | head -1 | sed 's/.*(\([^)]*\)).*/\1/' | head -1)
          echo "Using simulator: $SIMULATOR"
          
          xcodebuild build \
            -project CribAlert.xcodeproj \
            -scheme CribAlert \
            -destination "platform=iOS Simulator,id=$SIMULATOR" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | xcpretty --color || {
              echo "Build failed with xcpretty, showing raw output..."
              xcodebuild build \
                -project CribAlert.xcodeproj \
                -scheme CribAlert \
                -destination "platform=iOS Simulator,id=$SIMULATOR" \
                -configuration Debug \
                CODE_SIGN_IDENTITY="" \
                CODE_SIGNING_REQUIRED=NO \
                CODE_SIGNING_ALLOWED=NO
            }

      - name: Validate no prohibited medical claims
        run: |
          echo "🔍 Scanning for prohibited medical CLAIMS..."
          cd CribAlert/CribAlert
          
          # We're looking for POSITIVE claims, not disclaimers
          # Disclaimers like "does not monitor vital signs" are REQUIRED
          # Claims like "monitors breathing" or "detects oxygen" are PROHIBITED
          
          FOUND_ISSUES=0
          
          # Check for claims of breathing detection (prohibited)
          if grep -riE "detect.*breathing|monitor.*breathing|track.*breathing|measure.*breathing" --include="*.swift" .; then
            echo "❌ ERROR: Found breathing detection claim"
            FOUND_ISSUES=1
          fi
          
          # Check for claims of oxygen/vitals monitoring (prohibited)
          if grep -riE "detect.*oxygen|monitor.*oxygen|track.*oxygen|measure.*oxygen" --include="*.swift" .; then
            echo "❌ ERROR: Found oxygen monitoring claim"
            FOUND_ISSUES=1
          fi
          
          # Check for claims of heart rate monitoring (prohibited)
          if grep -riE "detect.*heart|monitor.*heart|track.*heart|measure.*heart" --include="*.swift" .; then
            echo "❌ ERROR: Found heart rate monitoring claim"
            FOUND_ISSUES=1
          fi
          
          # Check for SIDS prevention claims (prohibited) - but NOT disclaimers
          if grep -riE "prevent.*SIDS|stop.*SIDS|protect.*SIDS" --include="*.swift" .; then
            echo "❌ ERROR: Found SIDS prevention claim"
            FOUND_ISSUES=1
          fi
          
          # Check for life-saving claims (prohibited)
          if grep -riE "life.saving|lifesaving|save.*life|saves.*lives" --include="*.swift" .; then
            echo "❌ ERROR: Found life-saving claim"
            FOUND_ISSUES=1
          fi
          
          # Check for emergency terminology (prohibited in alerts)
          if grep -riE '"emergency"|"EMERGENCY"|alertTitle.*emergency' --include="*.swift" .; then
            echo "❌ ERROR: Found emergency terminology in user-facing strings"
            FOUND_ISSUES=1
          fi
          
          if [ $FOUND_ISSUES -eq 0 ]; then
            echo "✅ No prohibited medical claims found"
            echo "ℹ️  Note: Disclaimers like 'does not monitor vital signs' are allowed and required"
          else
            echo "❌ Build failed due to medical language violations"
            exit 1
          fi

      - name: Validate privacy claims
        run: |
          echo "🔒 Validating privacy implementation..."
          cd CribAlert/CribAlert
          
          # Check that we're not uploading video/audio anywhere
          if grep -riE "upload.*video|upload.*audio|send.*video|send.*audio|POST.*video|POST.*audio" --include="*.swift" .; then
            echo "⚠️ WARNING: Found potential video/audio upload code. Verify this matches privacy claims."
          else
            echo "✅ No video/audio upload code found"
          fi
          
          # Check for analytics/tracking (should not exist in V1)
          if grep -riE "Analytics|Tracking|Firebase|Amplitude|Mixpanel" --include="*.swift" .; then
            echo "⚠️ WARNING: Found analytics/tracking code. Verify this is intentional."
          else
            echo "✅ No analytics/tracking code found"
          fi

  lint:
    name: Swift Lint
    runs-on: macos-14
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: |
          cd CribAlert
          swiftlint lint --reporter github-actions-logging || true
        continue-on-error: true  # Don't fail build on lint warnings for now

  test:
    name: Run Unit Tests
    runs-on: macos-14
    needs: build  # Only run if build succeeds
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode version
        run: |
          if [ -d "/Applications/Xcode_15.4.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.3.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.3.app/Contents/Developer
          elif [ -d "/Applications/Xcode_15.2.app" ]; then
            sudo xcode-select -s /Applications/Xcode_15.2.app/Contents/Developer
          elif [ -d "/Applications/Xcode.app" ]; then
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          fi

      - name: Install xcpretty
        run: gem install xcpretty

      - name: Run Tests
        run: |
          cd CribAlert
          
          # Find an available iPhone simulator
          SIMULATOR=$(xcrun simctl list devices available | grep "iPhone" | head -1 | sed 's/.*(\([^)]*\)).*/\1/' | head -1)
          
          xcodebuild test \
            -project CribAlert.xcodeproj \
            -scheme CribAlert \
            -destination "platform=iOS Simulator,id=$SIMULATOR" \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | xcpretty --color || true
        continue-on-error: true  # Tests may not exist yet
